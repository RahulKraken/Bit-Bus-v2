<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="350dp"
    android:layout_height="380dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".Fragments.NextStudentBusFragment">

    <LinearLayout
        android:id="@+id/nextStudentBusLinearLayout"
        android:orientation="vertical"
        android:background="@drawable/next_student_bus_bg"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:gravity="top|start"
        android:paddingStart="30dp"
        android:paddingTop="50dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/nextStudentBusLabel"
            android:text="@string/next_student_bus_label"
            android:textSize="34sp"
            android:textColor="@android:color/black"
            android:textStyle="bold"
            android:padding="5dp"
            android:maxLines="2"
            android:layout_width="200dp"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/nextStudentBusTime"
            android:text="@string/next_student_bus_placeholder_time"
            android:textSize="44sp"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:paddingStart="5dp"
            android:paddingEnd="25dp"
            android:paddingTop="5dp"
            android:paddingBottom="5dp"
            android:layout_width="200dp"
            android:textAlignment="viewStart"
            android:maxLines="2"
            android:layout_height="wrap_content" />

    </LinearLayout>

</android.support.constraint.ConstraintLayout>